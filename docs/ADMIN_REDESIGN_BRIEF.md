# Design brief — CampusCart platform admin console

Paste the block below into Claude Design. Everything in it is drawn from the
live codebase (`apps/admin`), so the resulting design can be implemented
without inventing API surface.
