# Dependencies
node_modules

# Secrets — never commit; see .env.example for the template
.env
.env.local
.env.*.local

# Build output
dist
build
.expo
.expo-shared

# Logs
*.log

# Generated migration metadata
drizzle/meta

# Runtime user uploads
apps/api/uploads/*
!apps/api/uploads/.gitkeep

# Core dumps / crash artifacts
core
core.*

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp

# TypeScript incremental build cache
*.tsbuildinfo

# Demo web build (regenerate with: expo export --platform web)
apps/mobile/dist
