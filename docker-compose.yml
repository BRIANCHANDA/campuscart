services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: campuscart
      POSTGRES_PASSWORD: campuscart
      POSTGRES_DB: campuscart
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
